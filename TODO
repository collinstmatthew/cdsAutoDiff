add in start date instead of 0 will need to divide by this discouunt factor in more place now as well then

move accrued interest into cash flow function

use the getVal function on the curve and this should clean things up a bit

implement proper day couunt convention

make Curves Mondoids so we can take the difference of them etc
also allow curve to be multipled by a scalar, correct structure for this?

Then make marketes monoids
And also allow scalar multiplication by a number

